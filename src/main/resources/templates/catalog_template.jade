doctype html
html
    head
        title= Catalog
    body
        script(type="application/ld+json") #{snippet}
        h1 Catalog
        table
            tbody
                each part in parts
                    tr
                        td Part - #{part.sku}
                        td
                            a(href="#{part.url}") Click here

